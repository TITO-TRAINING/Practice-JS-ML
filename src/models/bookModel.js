class Book {
  constructor(id, title, author, genre, publishedYear) {
    this.id = id;
    this.title = title;
    this.author = author;
    this.genre = genre;
    this.publishedYear = publishedYear;
  }
}

export default Book;
